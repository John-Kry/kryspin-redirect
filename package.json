{
  "name": "kryspin-redirect",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-deploy": "npm install && cd client && npm install && npm run build && pm2 restart redirect_service"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.19.2",
    "body-parser": "^1.20.1",
    "dotenv": "^8.2.0",
    "express": "^4.18.2",
    "is-url": "^1.2.4",
    "pg": "^8.2.1"
  }
}